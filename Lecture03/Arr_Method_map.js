
const final = ["Taylor","Donald","Don","Natacha","Booby"]

const announ = final.map((member)=>{
    return member + " joined the contest\n"
})

console.log(announ)


/*
const KvArray = [
    { key: 1, value: 10},
    { key: 2, value: 20},
    { key: 3, value: 30}
];

const reformat = KvArray.map(({ key, value }) => ({ [key]: value }))

console.log(reformat)
console.log(KvArray)
*/